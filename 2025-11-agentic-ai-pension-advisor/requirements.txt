# Databricks Apps installs dependencies from `requirements.txt` by default.
# To avoid version drift (e.g., older Streamlit in deployments), we make this file
# the single source of truth and include the app dependency set here.
-r requirements-app.txt
